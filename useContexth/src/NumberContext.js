import { createContext } from "react";

const Numbercontext = createContext();

export {Numbercontext};
//Numbercontext is something we are exporting
//default means we can export only one module


// create a NumberContext.js